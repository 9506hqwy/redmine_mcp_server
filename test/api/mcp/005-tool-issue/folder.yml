info:
  name: 005-tool-issue
  type: folder
  seq: 4

request:
  auth: inherit
  scripts:
    - type: before-request
      code: |-
        const _meta = {
          "_meta": {
            "io.modelcontextprotocol/protocolVersion": "2026-07-28",
            "io.modelcontextprotocol/clientCapabilities": {},
          }
        };

        const body = req.getBody();
        body.params = { ..._meta, ...body.params };
        req.setBody(body);
