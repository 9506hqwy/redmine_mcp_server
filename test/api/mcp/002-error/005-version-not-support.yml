info:
  name: 005-version-not-support
  type: http
  seq: 5

http:
  method: POST
  url: "{{targetUrl}}"
  headers:
    - name: MCP-Protocol-Version
      value: 0000-00-00
    - name: MCP-Method
      value: tools/list
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "id": 1,
        "method": "tools/list",
        "params": {
          "_meta": {
            "io.modelcontextprotocol/protocolVersion": "0000-00-00"
          }
        }
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.id
      operator: eq
      value: 1
    - expression: res.body.error.code
      operator: eq
      value: "-32022"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
